import "./globals.css";
import type { Metadata } from "next";
import { Inter } from "next/font/google";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Inscribe - AI-Powered Code Documentation Extension for VS Code",
  description:
    "Transform your code documentation workflow with Inscribe, an intelligent VS Code extension that generates comprehensive documentation using AI.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}